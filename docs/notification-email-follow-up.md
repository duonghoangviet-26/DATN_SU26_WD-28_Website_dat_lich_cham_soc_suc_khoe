# Notification Email Follow Up

Possible improvements after the current development milestone:

- Add retry queue for failed sends.
- Add delivery status per recipient.
- Add email templates with shared branding.
- Add bounce tracking with a transactional email provider.
- Add a dedicated notification email field only if login email and delivery email need to diverge.

Keep the current implementation simple until real reception, nurse, and doctor workflows are stable.
